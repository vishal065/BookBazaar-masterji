export enum OrderStatus {
    Pending = "pending",
    fullfilled = "fullfilled",
    Cancelled = "cancelled",
    Failed = "failed",
}


export enum PaymentStatus {
    Pending = "pending",
    Paid = "paid",
    Failed = "failed",
    Refunded = "refunded",
}